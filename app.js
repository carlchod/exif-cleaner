const translations = {
    en: {
        title: "EXIF Cleaner",
        subtitle: "Drag and drop as many images as you want. We analyze them and pack them cleaned into a ZIP. <br><span class='text-emerald-600 font-semibold bg-emerald-50 px-2 py-0.5 rounded-md mt-2 inline-block'>100% local in your browser.</span>",
        formatLabel: "Output Format",
        formatOriginal: "Keep Original",
        formatJpeg: "Convert to JPEG",
        formatWebp: "Convert to WebP (Web)",
        formatPng: "Convert to PNG",
        qualityLabel: "Image Quality",
        qualityHint: "PNG ignores this setting.",
        dropBoxText: "<span class='font-semibold text-blue-600'>Choose images</span> or drag them here",
        progressPrep: "Preparing...",
        resultsTitle: "Analysis Results:",
        btnWaiting: "Waiting for processing...",
        alertNoImage: "Please upload only image files (JPEG, PNG, WEBP).",
        statusStart: "Starting processing of {count} image(s)...",
        statusProcess: "Processing image {current} of {total}...",
        statusZip: "Creating ZIP archive...",
        statusDone: "✅ All images successfully cleaned!",
        btnDownload: "Download {count} image(s) as ZIP",
        gpsFound: "<span class='text-rose-500 font-bold'>GPS found!</span>",
        noGps: "<span class='text-emerald-500'>No GPS</span>",
        unknown: "Unknown",
        // NEUE TEXTE:
        feat1Title: "Zero Trust Privacy",
        feat1Desc: "Your images never leave your device. All metadata is stripped locally inside your browser.",
        feat2Title: "Lightning Fast",
        feat2Desc: "Clean hundreds of images in seconds. No uploading or downloading wait times.",
        feat3Title: "Format Conversion",
        feat3Desc: "Easily convert bulky smartphone photos into web-optimized WebP or JPEG files.",
        footerRights: "© 2026 EXIF Cleaner Pro. Built for privacy."
    },
    de: {
        title: "EXIF Cleaner",
        subtitle: "Zieh so viele Bilder rein, wie du willst. Wir packen sie analysiert und bereinigt in ein ZIP. <br><span class='text-emerald-600 font-semibold bg-emerald-50 px-2 py-0.5 rounded-md mt-2 inline-block'>100% lokal im Browser.</span>",
        formatLabel: "Ausgabeformat",
        formatOriginal: "Original behalten",
        formatJpeg: "Zu JPEG konvertieren",
        formatWebp: "Zu WebP konvertieren",
        formatPng: "Zu PNG konvertieren",
        qualityLabel: "Bildqualität",
        qualityHint: "PNG ignoriert dies.",
        dropBoxText: "<span class='font-semibold text-blue-600'>Bilder auswählen</span> oder reinziehen",
        progressPrep: "Bereite vor...",
        resultsTitle: "Analyse-Ergebnisse:",
        btnWaiting: "Warte auf Bilder...",
        alertNoImage: "Bitte nur Bilder (JPEG, PNG, WEBP) hochladen.",
        statusStart: "Starte Verarbeitung: {count} Bild(er)...",
        statusProcess: "Verarbeite Bild {current} von {total}...",
        statusZip: "Erstelle ZIP-Archiv...",
        statusDone: "✅ Alle Bilder bereinigt!",
        btnDownload: "{count} Bild(er) als ZIP laden",
        gpsFound: "<span class='text-rose-500 font-bold'>GPS gefunden!</span>",
        noGps: "<span class='text-emerald-500'>Kein GPS</span>",
        unknown: "Unbekannt",
        // NEUE TEXTE:
        feat1Title: "Zero Trust Datenschutz",
        feat1Desc: "Deine Bilder verlassen nie dein Gerät. Alle Metadaten werden lokal in deinem Browser gelöscht.",
        feat2Title: "Pfeilschnell",
        feat2Desc: "Bereinige hunderte Bilder in Sekunden. Keine nervigen Upload- oder Download-Wartezeiten.",
        feat3Title: "Format-Konverter",
        feat3Desc: "Konvertiere riesige Smartphone-Fotos direkt in speicherplatzsparende WebP- oder JPEG-Dateien.",
        footerRights: "© 2026 EXIF Cleaner Pro. Gebaut für Privatsphäre."
    },
    es: {
        title: "EXIF Cleaner",
        subtitle: "Arrastra tantas imágenes como quieras. Las empaquetamos limpias en un ZIP. <br><span class='text-emerald-600 font-semibold bg-emerald-50 px-2 py-0.5 rounded-md mt-2 inline-block'>100% local en tu navegador.</span>",
        formatLabel: "Formato de salida",
        formatOriginal: "Mantener Original",
        formatJpeg: "Convertir a JPEG",
        formatWebp: "Convertir a WebP",
        formatPng: "Convertir a PNG",
        qualityLabel: "Calidad de imagen",
        qualityHint: "PNG ignora esto.",
        dropBoxText: "<span class='font-semibold text-blue-600'>Elige imágenes</span> o arrástralas",
        progressPrep: "Preparando...",
        resultsTitle: "Resultados:",
        btnWaiting: "Esperando imágenes...",
        alertNoImage: "Sube solo imágenes (JPEG, PNG, WEBP).",
        statusStart: "Procesando {count} imagen(es)...",
        statusProcess: "Imagen {current} de {total}...",
        statusZip: "Creando ZIP...",
        statusDone: "✅ ¡Todas limpias!",
        btnDownload: "Descargar {count} imagen(es)",
        gpsFound: "<span class='text-rose-500 font-bold'>¡GPS encontrado!</span>",
        noGps: "<span class='text-emerald-500'>Sin GPS</span>",
        unknown: "Desconocido",
        // NEUE TEXTE:
        feat1Title: "Privacidad Zero Trust",
        feat1Desc: "Tus imágenes nunca salen de tu dispositivo. Todos los datos se borran localmente.",
        feat2Title: "Súper Rápido",
        feat2Desc: "Limpia cientos de imágenes en segundos. Sin tiempos de subida ni descarga.",
        feat3Title: "Conversión de Formato",
        feat3Desc: "Convierte fotos pesadas en archivos WebP o JPEG optimizados para la web.",
        footerRights: "© 2026 EXIF Cleaner Pro. Construido para la privacidad."
    },
    fr: {
        title: "EXIF Cleaner",
        subtitle: "Déposez autant d'images que vous voulez. Nous les regroupons nettoyées dans un ZIP. <br><span class='text-emerald-600 font-semibold bg-emerald-50 px-2 py-0.5 rounded-md mt-2 inline-block'>100% local dans le navigateur.</span>",
        formatLabel: "Format de sortie",
        formatOriginal: "Conserver l'original",
        formatJpeg: "Convertir en JPEG",
        formatWebp: "Convertir en WebP",
        formatPng: "Convertir en PNG",
        qualityLabel: "Qualité d'image",
        qualityHint: "Ignoré pour PNG.",
        dropBoxText: "<span class='font-semibold text-blue-600'>Choisissez des images</span> ou glissez-les",
        progressPrep: "Préparation...",
        resultsTitle: "Résultats :",
        btnWaiting: "En attente...",
        alertNoImage: "Seulement des images (JPEG, PNG, WEBP).",
        statusStart: "Traitement de {count} image(s)...",
        statusProcess: "Image {current} sur {total}...",
        statusZip: "Création du ZIP...",
        statusDone: "✅ Toutes nettoyées !",
        btnDownload: "Télécharger {count} image(s)",
        gpsFound: "<span class='text-rose-500 font-bold'>GPS trouvé !</span>",
        noGps: "<span class='text-emerald-500'>Pas de GPS</span>",
        unknown: "Inconnu",
        // NEUE TEXTE:
        feat1Title: "Confidentialité Zero Trust",
        feat1Desc: "Vos images ne quittent jamais votre appareil. Les métadonnées sont supprimées localement.",
        feat2Title: "Ultra Rapide",
        feat2Desc: "Nettoyez des centaines d'images en quelques secondes. Aucun temps de téléchargement.",
        feat3Title: "Conversion de Format",
        feat3Desc: "Convertissez facilement vos photos en fichiers WebP ou JPEG optimisés pour le web.",
        footerRights: "© 2026 EXIF Cleaner Pro. Conçu pour la confidentialité."
    }
};

let currentLang = 'en';

// ==========================================
// DOM ELEMENTS
// ==========================================
const langSelect = document.getElementById('langSelect');
const imageUpload = document.getElementById('imageUpload');
const dropzone = document.getElementById('dropzone');
const resultArea = document.getElementById('resultArea');
const downloadBtn = document.getElementById('downloadBtn');
const logList = document.getElementById('logList');
const progressStatus = document.getElementById('progressStatus');
const progressPercentage = document.getElementById('progressPercentage');
const progressBar = document.getElementById('progressBar');
const formatSelect = document.getElementById('formatSelect');
const qualitySlider = document.getElementById('qualitySlider');
const qualityLabel = document.getElementById('qualityLabel');

let generatedZipBlob = null;

// ==========================================
// LANGUAGE SWITCHER LOGIC
// ==========================================
function setLanguage(lang) {
    currentLang = lang;
    document.documentElement.lang = lang; // Update HTML lang tag
    
    // Update all static text elements
    document.querySelectorAll('[data-i18n]').forEach(element => {
        const key = element.getAttribute('data-i18n');
        if (translations[lang][key]) {
            element.innerHTML = translations[lang][key];
        }
    });
}

langSelect.addEventListener('change', (e) => {
    setLanguage(e.target.value);
});

// Helper function to get text
function t(key, params = {}) {
    let text = translations[currentLang][key] || key;
    for (const [paramKey, paramValue] of Object.entries(params)) {
        text = text.replace(`{${paramKey}}`, paramValue);
    }
    return text;
}

// ==========================================
// UI LOGIC
// ==========================================
qualitySlider.addEventListener('input', function(e) {
    qualityLabel.textContent = `${e.target.value}%`;
});

// Drag & Drop Event-Handling
['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
    dropzone.addEventListener(eventName, preventDefaults, false);
});

function preventDefaults(e) {
    e.preventDefault();
    e.stopPropagation();
}

['dragenter', 'dragover'].forEach(eventName => {
    dropzone.addEventListener(eventName, () => {
        dropzone.classList.add('border-blue-500', 'bg-blue-50');
    }, false);
});

['dragleave', 'drop'].forEach(eventName => {
    dropzone.addEventListener(eventName, () => {
        dropzone.classList.remove('border-blue-500', 'bg-blue-50');
    }, false);
});

dropzone.addEventListener('drop', function(e) {
    const files = e.dataTransfer.files;
    if (files.length > 0) processFilesArray(files);
}, false);

imageUpload.addEventListener('change', function(event) {
    const files = event.target.files;
    if (files.length > 0) processFilesArray(files);
});

// ==========================================
// CORE LOGIC
// ==========================================
async function processFilesArray(files) {
    const validFiles = Array.from(files).filter(file => file.type.startsWith('image/'));
    
    if (validFiles.length === 0) {
        alert(t('alertNoImage'));
        return;
    }

    logList.innerHTML = '';
    resultArea.classList.remove('hidden');
    downloadBtn.disabled = true;
    downloadBtn.innerHTML = t('btnWaiting');
    
    progressBar.style.width = '0%';
    progressPercentage.textContent = '0%';
    progressStatus.textContent = t('statusStart', { count: validFiles.length });
    
    const zip = new JSZip();
    const targetFormat = formatSelect.value;
    const targetQuality = parseInt(qualitySlider.value) / 100;

    for (let i = 0; i < validFiles.length; i++) {
        progressStatus.textContent = t('statusProcess', { current: i + 1, total: validFiles.length });
        
        await processSingleFile(validFiles[i], zip, targetFormat, targetQuality);
        
        const percent = Math.round(((i + 1) / validFiles.length) * 100);
        progressBar.style.width = `${percent}%`;
        progressPercentage.textContent = `${percent}%`;
    }

    progressStatus.textContent = t('statusZip');
    
    zip.generateAsync({ type: "blob" }).then(function(content) {
        generatedZipBlob = content;
        downloadBtn.disabled = false;
        downloadBtn.textContent = t('btnDownload', { count: validFiles.length });
        progressStatus.textContent = t('statusDone');
    });
}

function processSingleFile(file, zip, targetFormat, targetQuality) {
    return new Promise((resolve) => {
        EXIF.getData(file, function() {
            const make = EXIF.getTag(this, "Make") || t('unknown');
            const model = EXIF.getTag(this, "Model") || "";
            const hasGPS = EXIF.getTag(this, "GPSLatitude") ? true : false;
            
            const li = document.createElement('li');
            const gpsText = hasGPS ? t('gpsFound') : t('noGps');
                
            li.innerHTML = `<strong>${file.name}</strong>: ${make} ${model} | ${gpsText}`;
            logList.appendChild(li);
        });

        const reader = new FileReader();
        reader.onload = function(e) {
            const img = new Image();
            img.onload = function() {
                const canvas = document.createElement('canvas');
                canvas.width = img.width;
                canvas.height = img.height;
                const ctx = canvas.getContext('2d');
                ctx.drawImage(img, 0, 0);

                let outputMimeType = targetFormat;
                if (outputMimeType === 'original') outputMimeType = file.type;
                if (!['image/jpeg', 'image/png', 'image/webp'].includes(outputMimeType)) outputMimeType = 'image/jpeg';

                const dataUrl = canvas.toDataURL(outputMimeType, targetQuality);
                const base64Data = dataUrl.replace(/^data:image\/(png|jpeg|webp);base64,/, "");
                
                let extension = outputMimeType.split('/')[1];
                if (extension === 'jpeg') extension = 'jpg';
                
                const cleanName = file.name.split('.').slice(0, -1).join('.') + '_cleaned.' + extension;
                
                zip.file(cleanName, base64Data, {base64: true});
                resolve();
            };
            img.src = e.target.result;
        };
        reader.readAsDataURL(file);
    });
}

downloadBtn.addEventListener('click', function() {
    if (!generatedZipBlob) return;
    const link = document.createElement('a');
    link.href = URL.createObjectURL(generatedZipBlob);
    link.download = "cleaned_images.zip";
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
});s

// Initialize default language on page load
setLanguage(currentLang);